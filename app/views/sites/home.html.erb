<div class="jumbotron hero text-right text-white pb-5">
  <div class="container-fluid mt-5 pt-5">
    <h1 class="display-4">¡Comienza tu camino de la espada!</h1>
    <p class="lead">Kendo, Iaido y Jodo, únete a un club de la Federación Chilena de Kendo</p>

    <div class="pt-5">
      <%= link_to 'Conoce más, ¡únete a uno de nuestros clubes!', posts_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
</div>

<div class="container">
  <h1>Últimas Noticias</h1>

  <div class="card-deck">
    <% @posts.each do |post| %>
      <div class="col-md-12 col-lg-4 d-flex align-items-stretch py-2">
        <div class="card">
          <img src="<%= post.photo.postimg%>" class="card-img-top" alt="...">
          <div class="card-body">
            <h5 class="card-title"><strong><%= post.title %></strong></h5>
            <p class="card-text"><%= post.description.truncate(180, separator: /\s/)%> <%= link_to 'Leer más', post, class:'stretched-link'%></p>
            <div>
              <!-- poner tags acá -->
              <% post.tags.each do |tag| %>
                <small>#<%= tag.name %></small>
              <% end %>
            </div>
            <div class="row">
              <div class="col-7">
                <small class="text-muted"><%=time_ago_in_words(post.created_at) %></small>
              </div>
              <div class="col-5">
                <small class="text-muted">N° Comentarios: <%= post.comments.size %></small>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  </br>
  <div class="text-center">
    <%= link_to 'Más Noticias', posts_path, class: 'btn btn-outline-primary' %>
  </div>


  <br>
</div>
